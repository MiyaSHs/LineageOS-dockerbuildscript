# VARIABLES - saved config for LineageOS docker builds
# This file is sourced by main/build scripts.

DEVICE_LIST=""
BRANCH_NAME=""

# microG related: no | restricted | yes
SIGNATURE_SPOOFING="restricted"

# true | false
SIGN_BUILDS="true"

# e.g. 50G, 100G
CCACHE_SIZE="100G"

# integer, e.g. 4, 8, 16
PARALLEL_JOBS="2"

# Outputs a .zip image (installable via recovery or adb sideload)
ZIP_UP_IMAGES="true"

### >>> change this to true if you want to create a local mirror of the LineageOS source (> 200 GB)
LOCAL_MIRROR="false"

# if you want it to always reset/clean kernel + partner_gms every time, even if clean
REPOCLEAN_FORCE=true
